# Project-Mickey
Cross platform desktop gym management application written in Java and Swing UI framework.  It uses 3M Cognet fingerprint reader for user identification and authentication but can be configured to use fingerprint readers from other vendors also
